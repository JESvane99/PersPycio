[project]
name = "perspycio"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jonas E. Svane", email = "jonassvane9@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "lmfit>=1.3.4",
]

[build-system]
requires = ["uv_build>=0.9.28,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
]
